# PhysicsEngine2D

> AI Attribution / AI 声明: This project is fully generated by AI (architecture, code, tests, and documentation). / 本项目完全由 AI 生成（架构、代码、测试与文档）。

Interactive 2D physics engine project in C.  
一个使用 C 语言实现的交互式 2D 物理引擎项目。

A C99 2D rigid-body physics engine with a Windows Direct2D sandbox app and regression tests.  
基于 C99 的 2D 刚体物理引擎，包含 Windows Direct2D 可视化沙盒程序与回归测试。

## About / 关于

C99 2D rigid-body physics engine with an interactive Windows sandbox, scene presets, and regression tests.  
基于 C99 的 2D 刚体物理引擎，提供可交互的 Windows 沙盒、场景预设与回归测试。

## GitHub Metadata / GitHub 元数据

- Suggested description / 建议描述: C99 2D rigid-body physics engine with an interactive Windows sandbox, scene presets, and regression tests.
- Suggested topics / 建议标签: `physics-engine`, `2d-physics`, `rigid-body`, `collision-detection`, `impulse-solver`, `c99`, `direct2d`, `directwrite`, `windows`, `sandbox`

The repository is organized around a visual sandbox workflow:  
仓库围绕可视化沙盒工作流组织：
- engine core / 引擎内核 (`src/`, `include/`)
- interactive app / 交互程序 (`apps/sandbox_dwrite/`)
- regression checks / 回归验证 (`tests/`)

Build output is explicitly split:  
构建产物明确拆分为：
- `lib/libphysics2d.a` as reusable core library / 可复用核心静态库
- `bin/physics_sandbox(.exe)` as editor app / 编辑器（沙盒）应用

## Project Structure / 项目结构

```text
.
|-- apps/
|   `-- sandbox_dwrite/
|       `-- main.c
|-- include/
|   |-- body.h
|   |-- collision.h
|   |-- physics.h
|   |-- physics_math.h
|   `-- shape.h
|-- src/
|   |-- body.c
|   |-- collision.c
|   |-- math.c
|   |-- physics.c
|   `-- shape.c
|-- tests/
|   `-- regression_tests.c
|-- CMakeLists.txt
`-- Makefile
```

## Current Focus / 当前重点

`apps/sandbox_dwrite/main.c` is the main entry point.  
`apps/sandbox_dwrite/main.c` 是当前主入口。

It provides / 功能包括：
- Play/Pause/Single-step simulation / 播放、暂停、单步模拟
- Mouse pick and drag for dynamic bodies / 鼠标抓取拖拽动态刚体
- Spawn circle/box at cursor / 在鼠标处生成圆形或方形
- Scene presets and hot switching (`F1-F9`, `[`, `]`) / 场景预设与热切换
- Chinese UI modals: `配置` and `使用说明` / 中文 UI 模态框
- Per-scene config editing in config modal (`+/-` disabled at limit) / 每场景配置编辑（达上限禁用 `+/-`）
- Contact point and normal visualization / 接触点与法线可视化
- Velocity vector visualization / 速度向量可视化
- Runtime tuning for gravity/time step/damping/iterations / 运行时调节重力、时间步长、阻尼、迭代次数
- Out-of-stage recycle counter and recycle protection at spawn / 出界回收计数与生成保护
- Configurable UI font path via `sandbox_ui.ini` / 通过 `sandbox_ui.ini` 配置 UI 字体路径

## Build / 构建

### CMake (recommended) / CMake（推荐）

Build sandbox only / 仅构建沙盒：

```bash
cmake -S . -B build -DBUILD_SANDBOX=ON -DBUILD_TESTS=OFF
cmake --build build
./build/bin/physics_sandbox
```

Build with regression tests / 构建并包含回归测试：

```bash
cmake -S . -B build -DBUILD_SANDBOX=ON -DBUILD_TESTS=ON
cmake --build build
./build/bin/physics_tests
```

### Make

```bash
make core         # build reusable physics core static library / 构建可复用核心静态库
make              # same as: make sandbox / 等同 make sandbox
make run          # run sandbox / 运行沙盒
make test         # build + run regression tests / 构建并运行回归测试
make check-core   # run core-maintenance checklist / 运行内核维护检查
```

## Dependencies / 依赖

- C compiler with C99 support / 支持 C99 的 C 编译器
- Windows SDK libraries for Direct2D/DirectWrite (`d2d1`, `dwrite`, `ole32`, `uuid`, `shcore`, `gdi32`, `user32`) / Direct2D 与 DirectWrite 相关 Windows SDK 库

For MSYS2 UCRT64 / MSYS2 UCRT64 安装命令：

```bash
pacman -S --needed mingw-w64-ucrt-x86_64-gcc mingw-w64-ucrt-x86_64-make
```

## Reuse Core In Other Apps / 在其他应用中复用内核

1. Build core library / 先构建核心库：

```bash
make core
```

2. In another app, include headers from `include/` and link / 在其他应用中包含头文件并链接：

```bash
gcc your_app.c -I path/to/physicsengine2d-sandbox/include path/to/physicsengine2d-sandbox/lib/libphysics2d.a -lm
```

## Core Maintenance Docs / 内核维护文档

- Core development rules / 内核开发规则: [CORE_DEV_GUIDE.md](CORE_DEV_GUIDE.md)
- Change history / 变更历史: [CHANGELOG.md](CHANGELOG.md)
- Regression test template / 回归测试模板: [tests/REGRESSION_TEST_TEMPLATE.md](tests/REGRESSION_TEST_TEMPLATE.md)
- Chinese API doc / 中文 API 文档: [docs/API_CN.md](docs/API_CN.md)
- English API doc / 英文 API 文档: [docs/API_EN.md](docs/API_EN.md)

## Packaging (Windows) / Windows 打包

Portable zip / 便携压缩包：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\package.ps1 -Build -Version 1.0.0
```

Portable zip + installer (Inno Setup required) / 便携包 + 安装包（需 Inno Setup）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\package.ps1 -Build -WithInstaller -Version 1.0.0
```

Installer script path / 安装脚本路径：

`installer/physics_sandbox.iss`
