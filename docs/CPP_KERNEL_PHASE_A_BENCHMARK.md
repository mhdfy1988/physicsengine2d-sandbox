# 物理内核阶段 A 性能记录（C++ 内核）

更新时间：2026-03-05  
分支：`cpp-migration-baseline`

## 环境与命令
1. 命令：`mingw32-make benchmark`
2. 输出：
   - `avg_step_ms=0.2217`
   - `gate=8.0000`
   - 结果：`[PASS] benchmark gate`

## 结论
1. 当前 C++ 内核在现有基准门限下通过。
2. 该记录可作为后续阶段 B/C 的回归参考点。

## 说明
1. 当前仓库已完成 C++ 迁移，未在同一提交面保存“旧 C 内核”可直接复跑的对照基线。
2. 如需严格 C vs C++ 对比，需要从迁移前基线分支复测并补一份对照表。
